# Server Configuration
SERVER_PORT=8080

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=socialmeli
DB_SSLMODE=disable

# JWT Configuration
# IMPORTANTE: Troque esta chave por uma chave secreta forte em produção
# Você pode gerar uma chave segura com: openssl rand -base64 32
JWT_SECRET=your-secret-key-change-this-in-production
